# Registry

Model registry and experiment-tracking integrations belong here.

MLflow is the preferred starting point for experiment metadata, model versions, and artifact references. Keep actual model binaries out of normal Git.
