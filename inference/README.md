# Inference

Reusable model-loading, prediction, batch inference, and serving helpers belong here.

Application-specific UI/API code should generally live in the application repository rather than here.
